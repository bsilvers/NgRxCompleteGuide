import * as AuthActions from './auth.actions';
// allows us to us all the authentication actions just from importing AuthActions
// by grouping the authentication actions together
export {AuthActions};